{
  "name": "pizzadamico-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build:vite": "vite build",
    "build": "node scripts/build-inject.js",
    "deploy": "node scripts/deploy-sftp.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0",
    "@playwright/test": "^1.48.0",
    "esbuild": "^0.24.0",
    "ssh2-sftp-client": "^11.0.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "lenis": "^1.1.13",
    "gsap": "^3.12.5"
  }
}
